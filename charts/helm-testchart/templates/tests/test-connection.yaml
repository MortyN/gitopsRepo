apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-testchart.fullname" . }}-test-connection"
  labels:
    {{- include "helm-testchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-testchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
