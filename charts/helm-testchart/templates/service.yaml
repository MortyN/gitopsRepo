apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-testchart.fullname" . }}
  labels:
    {{- include "helm-testchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-testchart.selectorLabels" . | nindent 4 }}
